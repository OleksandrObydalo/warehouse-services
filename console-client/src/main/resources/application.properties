# Console Client Configuration
# All requests now go through API Gateway (Single Entry Point)
api.gateway.url=http://localhost:8080

# Disable web server (Console Client doesn't need one)
spring.main.web-application-type=none
