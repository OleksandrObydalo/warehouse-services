# Web Client Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                    http://localhost:8090                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP Requests
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                    WEB CLIENT APPLICATION                        │
│                        (Port 8090)                               │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    PRESENTATION LAYER                     │   │
│  │  ┌────────────────────────────────────────────────────┐  │   │
│  │  │         Thymeleaf Templates (Views)                │  │   │
│  │  │  - index.html (Home)                               │  │   │
│  │  │  - places/free.html                                │  │   │
│  │  │  - orders/create.html                              │  │   │
│  │  │  - orders/list.html                                │  │   │
│  │  │  - orders/details.html ⭐ (Complex Aggregation)   │  │   │
│  │  │  - payments/list.html                              │  │   │
│  │  │  - error.html                                      │  │   │
│  │  └────────────────────────────────────────────────────┘  │   │
│  └──────────────────────┬───────────────────────────────────┘   │
│                         │                                        │
│                         │ Model Data                             │
│                         │                                        │
│  ┌──────────────────────┴───────────────────────────────────┐   │
│  │                   CONTROLLER LAYER                       │   │
│  │  - HomeController                                        │   │
│  │  - PlaceController                                       │   │
│  │  - OrderController ⭐ (Complex Aggregation Logic)       │   │
│  │  - PaymentController                                     │   │
│  └──────────────────────┬───────────────────────────────────┘   │
│                         │                                        │
│                         │ Service Calls                          │
│                         │                                        │
│  ┌──────────────────────┴───────────────────────────────────┐   │
│  │                    SERVICE LAYER                         │   │
│  │         WarehouseWebService (API Client)                 │   │
│  │                                                           │   │
│  │  - getFreePlaces()                                       │   │
│  │  - createOrder()                                         │   │
│  │  - getAllOrders()                                        │   │
│  │  - getOrderById() ───┐                                   │   │
│  │  - confirmOrder()    │  Used together for                │   │
│  │  - getPaymentsByOrderId() ──┘  Complex Aggregation ⭐   │   │
│  │                                                           │   │
│  │  Uses: RestTemplate for HTTP communication               │   │
│  └──────────────────────┬───────────────────────────────────┘   │
│                         │                                        │
│  ┌──────────────────────┴───────────────────────────────────┐   │
│  │                EXCEPTION HANDLING LAYER                  │   │
│  │          GlobalExceptionHandler (@ControllerAdvice)      │   │
│  │  - Handles 4xx/5xx errors                                │   │
│  │  - Provides user-friendly messages                       │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────┬───────────────────────────────────────────┘
                        │
                        │ HTTP Requests to Gateway
                        │ (http://localhost:8080/api/*)
                        │
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                        API GATEWAY                               │
│                       (Port 8080)                                │
│                                                                   │
│  Routes requests to appropriate microservices:                   │
│  - /api/places/*   → Place Service                              │
│  - /api/orders/*   → Order Service                              │
│  - /api/payments/* → Payment Service                            │
│                                                                   │
└───────────┬──────────────────┬───────────────────┬──────────────┘
            │                  │                   │
            ↓                  ↓                   ↓
    ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
    │    PLACE     │   │    ORDER     │   │   PAYMENT    │
    │   SERVICE    │   │   SERVICE    │   │   SERVICE    │
    │  (Port 8081) │   │  (Port 8082) │   │  (Port 8083) │
    └──────────────┘   └──────────────┘   └──────────────┘
            │                  │                   │
            └──────────────────┴───────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │  DISCOVERY SERVICE │
                    │  (Eureka - 8761)   │
                    └────────────────────┘
```

## 🔄 Complex Aggregation Flow

### Order Details Page - Data Flow

```
User navigates to: /orders/{orderId}
         │
         ↓
┌────────────────────────────────────────────────┐
│     OrderController.showOrderDetails()         │
└────────────────────────────────────────────────┘
         │
         ├─────────────────────────────────┐
         │                                 │
         ↓                                 ↓
┌──────────────────────────┐    ┌──────────────────────────┐
│ STEP 1: Get Order Data   │    │ STEP 2: Get Payment Data │
│                          │    │                          │
│ warehouseService         │    │ warehouseService         │
│   .getOrderById()        │    │   .getPaymentsByOrderId()│
│         │                │    │         │                │
│         ↓                │    │         ↓                │
│   GET /api/orders/{id}   │    │ GET /api/payments/       │
│         │                │    │     order/{orderId}      │
│         ↓                │    │         │                │
│    API Gateway           │    │    API Gateway           │
│         │                │    │         │                │
│         ↓                │    │         ↓                │
│   Order Service          │    │   Payment Service        │
│    (Port 8082)           │    │    (Port 8083)           │
│         │                │    │         │                │
│         ↓                │    │         ↓                │
│   Returns OrderDTO       │    │ Returns List<PaymentDTO> │
└──────────┬───────────────┘    └────────────┬─────────────┘
           │                                 │
           └─────────────┬───────────────────┘
                         │
                         ↓
         ┌───────────────────────────────────┐
         │ STEP 3: Combine Data              │
         │                                   │
         │ OrderDetailsDTO orderDetails =    │
         │   new OrderDetailsDTO(            │
         │     order,      ← from Step 1     │
         │     payments    ← from Step 2     │
         │   );                              │
         └───────────────┬───────────────────┘
                         │
                         ↓
         ┌───────────────────────────────────┐
         │ STEP 4: Render View               │
         │                                   │
         │ model.addAttribute(               │
         │   "orderDetails", orderDetails);  │
         │                                   │
         │ return "orders/details";          │
         └───────────────┬───────────────────┘
                         │
                         ↓
         ┌───────────────────────────────────┐
         │  orders/details.html              │
         │                                   │
         │  ┌─────────────────────────────┐  │
         │  │ Order Information Card      │  │
         │  │ (from Order Service)        │  │
         │  │ - Order ID                  │  │
         │  │ - User ID                   │  │
         │  │ - Dates                     │  │
         │  │ - Racks                     │  │
         │  └─────────────────────────────┘  │
         │                                   │
         │  ┌─────────────────────────────┐  │
         │  │ Payment Information Card    │  │
         │  │ (from Payment Service)      │  │
         │  │ - Payment Status            │  │
         │  │ - Payment List              │  │
         │  │ - Amounts & Dates           │  │
         │  └─────────────────────────────┘  │
         │                                   │
         │  Both displayed on ONE page! ⭐  │
         └───────────────────────────────────┘
                         │
                         ↓
                   User's Browser
```

## 📊 Component Relationships

```
┌─────────────────────────────────────────────────────────┐
│                  WEB CLIENT COMPONENTS                   │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Controllers (4)                                         │
│  ├── HomeController ──────→ index.html                  │
│  ├── PlaceController ─────→ places/free.html            │
│  ├── OrderController ─────→ orders/create.html          │
│  │                      └─→ orders/list.html            │
│  │                      └─→ orders/details.html ⭐      │
│  └── PaymentController ───→ payments/list.html          │
│                                                          │
│  Service Layer (1)                                       │
│  └── WarehouseWebService                                │
│      ├── Uses: RestTemplate                             │
│      ├── Target: API Gateway (http://localhost:8080)    │
│      └── Methods:                                        │
│          ├── getFreePlaces()                            │
│          ├── createOrder()                              │
│          ├── getAllOrders()                             │
│          ├── getOrderById()          ┐                  │
│          ├── confirmOrder()          │  Complex         │
│          └── getPaymentsByOrderId()  ┘  Aggregation ⭐ │
│                                                          │
│  DTOs (4)                                                │
│  ├── PlaceDTO                                           │
│  ├── OrderDTO (with validation annotations)             │
│  ├── PaymentDTO                                         │
│  └── OrderDetailsDTO (aggregated) ⭐                    │
│                                                          │
│  Exception Handling (1)                                  │
│  └── GlobalExceptionHandler                             │
│      ├── @ExceptionHandler methods                      │
│      ├── Handles: 4xx, 5xx, Connection errors          │
│      └── Returns: error.html with friendly messages     │
│                                                          │
│  Configuration (1)                                       │
│  └── RestTemplateConfig                                 │
│      ├── Creates RestTemplate bean                      │
│      ├── Configures timeouts                            │
│      └── Sets up JSON serialization                     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## 🎨 UI Layout Structure

```
┌─────────────────────────────────────────────────────────┐
│                    NAVIGATION BAR                        │
│  [Logo] [Home] [Orders] [Free Places] [New Order]      │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│                                                          │
│                   SUCCESS/ERROR ALERTS                   │
│  (Displayed conditionally)                              │
│                                                          │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│                                                          │
│                                                          │
│                    MAIN CONTENT AREA                     │
│                                                          │
│  (Different content based on page)                      │
│                                                          │
│  - Home: Navigation cards                               │
│  - Free Places: Table of places                         │
│  - Create Order: Form                                   │
│  - Order List: Table of orders                          │
│  - Order Details: Order + Payment cards ⭐             │
│  - Payments: Table of payments                          │
│  - Error: Error message with tips                       │
│                                                          │
│                                                          │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│                        FOOTER                            │
│         Warehouse Management System © 2025              │
└─────────────────────────────────────────────────────────┘
```

## 🔀 Request/Response Flow

### Example: Creating an Order

```
1. User fills form and clicks "Create Order"
   ↓
2. Browser sends POST request to /orders/create
   ↓
3. OrderController.createOrder() receives request
   ↓
4. @Valid annotation triggers validation
   ├─→ If invalid: Return to form with errors
   └─→ If valid: Continue
   ↓
5. OrderController calls WarehouseWebService.createOrder()
   ↓
6. WarehouseWebService sends HTTP POST to Gateway
   Request: POST http://localhost:8080/api/orders
   Body: { userId, rackCount, desiredType, startDate, endDate }
   ↓
7. API Gateway routes to Order Service
   ↓
8. Order Service processes order
   ├─→ Checks available places
   ├─→ Assigns racks
   ├─→ Creates order
   └─→ Returns OrderDTO
   ↓
9. Gateway forwards response back
   ↓
10. WarehouseWebService receives OrderDTO
    ↓
11. OrderController processes response
    ├─→ Adds success message
    └─→ Redirects to order details page
    ↓
12. User sees order details (with complex aggregation)

If any error occurs (steps 6-9):
   ↓
GlobalExceptionHandler catches exception
   ↓
Extracts user-friendly message
   ↓
Returns error.html with message
   ↓
User sees friendly error (not stack trace) ✓
```

## 📐 Data Model

```
┌─────────────────────────────────────────────────────────┐
│                      PlaceDTO                            │
├─────────────────────────────────────────────────────────┤
│ - rackId: String                                        │
│ - sectionCode: String                                   │
│ - number: Integer                                       │
│ - type: String (STANDARD/COLD/HAZARDOUS)               │
│ - status: String                                        │
│ - pricePerDay: BigDecimal                              │
│ - dimensions: DimensionsDTO                            │
│ - tenantId: String                                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                      OrderDTO                            │
├─────────────────────────────────────────────────────────┤
│ - orderId: String                                       │
│ - userId: String (@NotBlank)                           │
│ - startDate: LocalDate (@NotNull)                      │
│ - endDate: LocalDate (@NotNull, @Future)               │
│ - rackCount: Integer (@NotNull, @Min(1))              │
│ - desiredType: String (@NotBlank)                      │
│ - assignedRacks: List<String>                          │
│ - status: String                                        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    PaymentDTO                            │
├─────────────────────────────────────────────────────────┤
│ - paymentId: String                                     │
│ - orderId: String                                       │
│ - userId: String                                        │
│ - amount: BigDecimal                                    │
│ - date: LocalDateTime                                   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│               OrderDetailsDTO ⭐                        │
├─────────────────────────────────────────────────────────┤
│ - order: OrderDTO           ← From Order Service        │
│ - payments: List<PaymentDTO> ← From Payment Service    │
│ - hasPaid: boolean          ← Computed                 │
│                                                          │
│ This is the AGGREGATED model that combines data         │
│ from TWO different microservices! ⭐                   │
└─────────────────────────────────────────────────────────┘
```

## 🎯 Key Design Patterns

1. **MVC Pattern**
   - Model: DTOs
   - View: Thymeleaf templates
   - Controller: Spring MVC Controllers

2. **Service Layer Pattern**
   - Controllers delegate to WarehouseWebService
   - Service handles all external communication

3. **DTO Pattern**
   - Data transfer between layers
   - Validation annotations on DTOs

4. **Template Method Pattern**
   - Thymeleaf templates with dynamic data

5. **Aggregation Pattern** ⭐
   - OrderDetailsDTO combines multiple service responses
   - Single view presents unified data

6. **Exception Handler Pattern**
   - @ControllerAdvice centralizes error handling
   - Converts technical errors to user-friendly messages

---

This architecture ensures:
- ✅ Clean separation of concerns
- ✅ Easy to maintain and extend
- ✅ User-friendly error handling
- ✅ Efficient communication with microservices
- ✅ Complex aggregation capability
- ✅ Professional UI/UX

